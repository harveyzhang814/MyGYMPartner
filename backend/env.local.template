# 本地开发环境配置模板
# 复制此文件为 .env 用于本地开发

NODE_ENV=development
PORT=3001

# 本地数据库配置 (PostgreSQL for development)
DATABASE_URL="postgresql://$(whoami)@localhost:5432/mygympartner_dev"

# JWT密钥 (仅用于本地开发)
JWT_SECRET=local-development-jwt-secret-key-12345

# CORS配置 (允许前端开发服务器)
CORS_ORIGIN=http://localhost:5173,http://localhost:3000

# 头像上传配置
# 开发环境：使用本地文件存储
# Staging/生产环境：使用 Supabase 存储
AVATAR_UPLOAD_ENABLED=true

# Supabase 配置（可选，开发环境可以不配置）
# 如果配置了，开发环境也可以使用 Supabase 存储
# SUPABASE_URL=https://xxx.supabase.co
# SUPABASE_ANON_KEY=eyJxxx...
# SUPABASE_SERVICE_ROLE_KEY=eyJxxx...
# SUPABASE_STORAGE_BUCKET=avatars-dev
